# C Programming Practice

September 15, 2026

---

# What I Learned & Did

* Practiced a C program to check whether three points are collinear and whether they can form a triangle.

* Calculated the three side lengths using the distance formula.

* Practiced classifying a triangle as equilateral, isosceles, or scalene.

* Practiced determining whether a triangle is right-angled, acute, or obtuse.

* Learned how to combine an `if / else if / else` ladder with nested `if` statements.

* Practiced computational biology.

* Practiced CSS.

---

# What Confused Me

* I was confused about why the angle-checking logic needs to first find the largest side and then check that side to determine whether the triangle is acute, right-angled, or obtuse.

* I was confused about how the outer `if / else if / else` ladder and the inner nested `if` statements work together. I understood that the outer conditions find the largest side, while the nested conditions determine the angle type.

* I questioned why `>=` is used instead of `>` when checking the largest side. I understood that `>=` also handles cases where two or all three sides are equal.

* I checked what happens when `AB = BC = CA` and understood why the first largest-side condition can still be true and the triangle is correctly classified as acute.

* I realized that my electricity bill program can be made much shorter and cleaner because some parts of the code are repetitive. I want to focus more on writing cleaner and less repetitive code in my upcoming programs.

---

# Next

* Try to write shorter and cleaner C programs instead of repeating the same logic.

* Practice more C programs involving nested `if` and `else if` logic.

* Continue with matrices and rank.

* Continue computational biology.

* Continue learning CSS.
