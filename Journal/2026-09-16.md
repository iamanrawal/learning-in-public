# C Programming Practice

September 16, 2026

---

# What I Learned & Did

* Practiced `if`, `else if`, and `else` using different C problems.
* Practiced a triangle validator using the triangle inequality conditions.
* Practiced nested `if` branching, where one `if` is placed inside another `if`.
* Worked on an electricity bill problem using different unit slabs and age-based discounts.
* Practiced using `&&` and `||` for multiple conditions.
* Practiced input validation using `scanf()` and `!=`.
* Learned how `scanf()` returns the number of successfully read inputs, such as `!= 3` when taking three values.
* Practiced understanding when to use `" "`, `%`, `,`, and `&` in `printf()` and `scanf()`.
* Did computational biology preparation.
* Worked on the boring biology assignment.

---

# What Confused Me

I kept forgetting the syntax for multiple inputs in `scanf()`, especially `scanf("%d %d %d",&a,&b,&c)` and why `!= 3` is used.

I also got confused about when to use `&`, `%`, commas, and quotation marks in `printf()` and `scanf()`. I learned that `%d` and `%f` specify the input or output type, `&` is normally used before variables in `scanf()`, commas separate arguments, and text and format specifiers go inside quotation marks.

I also understood the difference between a normal `if-else` and nested branching, where another `if` is placed inside an `if`.

---

# Next

* Practice more nested `if` and `else if` problems.
* Improve `scanf()` and `printf()` syntax.
* Practice more C problems with multiple conditions.
