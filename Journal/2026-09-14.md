# C Programming Practice

September 14, 2026

---

# What I Learned & Did

* Practiced an advanced electricity bill calculator using `if`, `else if`, and `else`.
* Learned how to calculate electricity bills using slab-wise calculations.
* Practiced combining multiple conditions using `&&` and `||`.
* Learned that `scanf()` returns the number of successfully taken inputs, so `!= 2` is used when taking two inputs.
* Practiced calculating discounts using `float`.
* Built an electricity bill program with age-based discounts of 0%, 5%, and 10%.
* Learned that the discount should be calculated from the bill amount, not the number of units.
* Practiced input validation for units and age.
* Did computational biology.
* Continued practicing C logic and syntax.

---

# What Confused Me

I was confused about how to calculate the electricity bill slab-wise. I learned that each range of units has its own rate and that the bill has to be calculated by combining the charges from each slab.

I was also confused about calculating the percentage discount. I learned that `0.05 * bill` gives a 5% discount and `0.10 * bill` gives a 10% discount.

While writing the program, I again made mistakes with combining conditions and learned to use `&&` when all conditions need to be true and `||` when either condition can be true.

I also learned that when `scanf()` takes two inputs, checking `!= 2` is better because both inputs need to be successfully read.

---

# Next

* Practice more advanced C programs using `if`, `else if`, and `else`.
* Practice more programs with multiple conditions.
* Continue with matrices and rank.
* Practice computational biology topics.

---

📌 Special Note: Went for dental cleaning related to my RCT, so I did not attend college today.
