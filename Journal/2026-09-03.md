# Git, HTML, C & AI/ML

September 3, 2026

---

# What I Learned

Today I continued learning HTML and studied the introduction to Generative AI, LLMs, SLMs, and MLLMs.

* Practiced `HTML` tables.
* Learned how to use `table`, `tr`, `th`, `td`, and `caption`.
* Practiced `colspan` and `rowspan`.
* Learned how `tfoot` is used in an HTML table.
* Studied the introduction to `Generative AI`.
* Learned basic concepts of `LLMs`, `SLMs`, and `MLLMs`.

---

# What I Did

* Practiced creating `HTML` tables in VS Code.
* Created a `Student Information` table.
* Practiced `colspan` and `rowspan`.
* Practiced using `tfoot` to display total students.
* Studied the basic concepts of `Generative AI`, `LLMs`, `SLMs`, and `MLLMs`.

---

# What Confused Me

I initially found `colspan` and `rowspan` confusing, especially understanding how they merge columns and rows in a table.

I also found `tfoot` confusing at first and learned that it contains `tr` and `th` or `td` to display information at the bottom of a table.

The differences between `LLMs`, `SLMs`, and `MLLMs` were also new concepts that I started understanding today.

---

# Next

* Continue learning `HTML`.
* Continue practicing `HTML` in VS Code.
* Start learning `Prompt Engineering`.
* Continue learning `C`.
* Practice the `Git Workflow`.
