# C Programming Practice

September 13, 2026

---

# What I Learned & Did

* Practiced a C program to check whether a number is divisible by 5.
* Practiced a C program to find the smaller of two numbers.
* Practiced finding the greatest of 3 numbers using `if`, `else if`, and `else`.
* Learned that `&&` is used when both conditions need to be true.
* Practiced checking whether two numbers are equal using `==`.
* Did matrices in maths.
* Practiced finding the rank of a matrix.
* Learned the basic rules of row echelon form.

---

# What Confused Me

I was confused about when to use `if`, `else if`, and `else`, especially about why `else` does not have a condition.

I also made a mistake using `=` instead of `==` while checking whether two numbers are equal. I learned that `=` assigns a value, while `==` compares two values.

While finding the greatest of 3 numbers, I was confused about how to combine conditions. I learned that && is used when both conditions need to be true. Also, there can be many ways of doing this, like you can use only 3 conditions, or you can also use 6 cases for a perfect one, which can have the same entries too.

In matrices, I was confused about pivots and why entries below a pivot need to be made 0. I learned that pivots must move to the right as we go down the rows.

I also learned that making pivots equal to `1` is not compulsory and is mainly done to make calculations easier.

---

# Next

* Practice more basic C programs using `if`, `else if`, and `else`.
* Practice more C programs with multiple conditions.
* Continue with matrices and rank.
* Practice converting matrices into row echelon form.

---

📌 Special Note: Still recovering from a tooth extraction, but managed to practice C and continue with matrices.
