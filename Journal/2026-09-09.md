# Git, HTML, C & CSS

September 9, 2026

---

# What I Learned & Did

Today I practiced `C`, worked on `HTML`, and continued `CSS`.

* Practiced `C` programs for weighted marks and Simple Interest.
* Learned about using `%f` with `float` values.
* Got confused about when to use `&` with `scanf()` and why it is not used the same way with `printf()`.
* Built a basic two-page university website using `HTML5`.
* Created `index.html` and `syllabus.html`.
* Practiced `strong`, `em`, lists, images, links, tables, `colspan`, audio, and video.
* Continued learning basic `CSS` selectors.
* Practiced the `Git Workflow`.

---

# What Confused Me

Got confused about `%f` and `&` in `scanf()` and `printf()`, especially why `&` is used with variables in `scanf()`.

---

# Next

* Continue `CSS`.
* Continue practicing `C`.
* Practice more `HTML`.
* Practice the `Git Workflow`.
